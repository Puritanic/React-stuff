# Pin scroll to bottom helper | React

> Example with managing scroll position in React

If using [yarn](https://yarnpkg.com) (recommended)

```
yarn install
yarn start
```

Otherwise:

```
npm install
npm start
```
